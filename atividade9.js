// 9. A função abaixo está com problema. Ela deveria retornar o valor 10, mas não retorna nada. Corrija-a:

// function calcularSoma(x, y) {
//   x + y;
// }


// resposta:
// Problema:
// A função está apenas somando x + y, mas não está retornando o resultado.

// Correção:

function calcularSoma(x, y) {
  return x + y;
} 

console.log(calcularSoma(5,5));
